# Reglas de TypeScript
- Todas las funciones exportadas deben incluir tipos de retorno explícitos.
- Prohibido el uso de `any` o `@ts-ignore`. Utilizar `unknown` con type guards en su lugar.
- Manejo de errores con Result patterns o bloques try/catch fuertemente tipados.
